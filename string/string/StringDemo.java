package string;

import java.util.Scanner;

public class StringDemo {
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);

        // String name = scan.next();  //take only single string.
        // String sentence = scan.nextLine();  //take whole sentence.

        // System.out.println(name);
        // System.out.println(sentence);

        /*
         * fullString = firstString+" "+secondString
         * 
         * 1. str.length() : give length of string.
         * 
         * 2. str.charAt(index) : print character of at given index.
         * 3. str.compareTo(name) : compare both string and retu
         * 
         * 
         * 
         */
        scan.close();
    }
}
